name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Type check
        run: npx vue-tsc -b

      - name: Build
        run: npm run build

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  # =========================================================
  # Optional: deploy to GitHub Pages
  # 1. Go to repo Settings → Pages → Source: "GitHub Actions"
  # 2. Uncomment the job below
  # 3. The project's Vite middleware endpoints won't work on Pages.
  #    You'll need a separate backend or remove those API calls.
  # =========================================================
  # deploy:
  #   if: github.ref == 'refs/heads/main'
  #   needs: build
  #   runs-on: ubuntu-latest
  #   permissions:
  #     pages: write
  #     id-token: write
  #   environment:
  #     name: github-pages
  #     url: ${{ steps.deployment.outputs.page_url }}
  #   steps:
  #     - uses: actions/deploy-pages@v4
